node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
coverage

# Rust / Tauri
src-tauri/target
src-tauri/gen

# Local data
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Secrets — never commit keys. (None are needed today; future keys belong in
# the macOS Keychain, not in files.)
.env
.env.*
!.env.example

# Logs and temporary files
*.log
logs/
*.tmp

# Mobile icon sets regenerated by `tauri icon`; this is a desktop app.
src-tauri/icons/android/
src-tauri/icons/ios/
