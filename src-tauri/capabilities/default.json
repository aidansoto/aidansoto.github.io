{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Local-only permissions. No network, filesystem or shell access is granted.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default"
  ]
}
